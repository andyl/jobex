#!/usr/bin/env bash 

mix git_ops.release --yes                  && \ 
git push --follow-tags                     && \ 
MIX_ENV=prod mix release --overwrite       && \ 
MIX_ENV=prod mix sysd.deploy               && \ 
git add .                                  && \ 
git commit -am'chore: update assets'       

